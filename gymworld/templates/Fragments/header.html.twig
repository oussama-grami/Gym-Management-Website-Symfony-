<!-- Header Section Begin -->
<header class="header-section">
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-3">
                <div class="logo">
                    <a href="{{ path('app_home') }}">
                        <img src="{{ asset('/assets/images/logo.png') }}" alt="app
                        logo" />
                    </a>
                </div>
            </div>
            <div class="col-lg-5">
                <nav class="nav-menu">
                    <ul>
                        <li><a href="{{ path('app_home') }}">Home</a></li>
                        <li></li>
                    </ul>
                </nav>
            </div>
            {# partie auth #}
            {% if 5==5 %}
            <div class="button-container col-lg-4 mt-5 mx-auto mt-lg-0 ms-lg-0">
                <a href="{{path('app_home') }}" class="primary-btn">Profile </a>
                <a href="{{ path('app_accesscard') }}" class="primary-btn">Access Card </a>
                <a href="{{ path('app_logout') }}" class="primary-btn">sign out </a>
            </div>
            {endif}
        </div>
        <div class="canvas-open">
            <i class="fa fa-bars"></i>
        </div>
    </div>
</header>
<!-- Header End -->